const injectable = require("injectable")

module.exports = injectable({
  route: "['apis', 'apisById'].length",
  get: function* (paths, apiRepository) {
    const count = yield apiRepository.find().count()
    return [{path: paths, value: count}]
  },
})
